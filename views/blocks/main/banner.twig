{% extends "block-base.twig" %}

{% block content %}

<div class="customBlockWrapper {{ block_class }} scroll-show" id="{{ block.id }}">
    <div class="customBlock">
        <div class="typo">
            {{ fields.text }}
        </div>
    </div>
</div>

{% endblock %}
