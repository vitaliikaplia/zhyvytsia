{% set post = Post(val) %}
{% set rating = fn('get_catalog_item_rating', val) %}
{% set price = post.meta('price') %}
{% set status = post.meta('status') %}
<div class="item scroll-show">
    <span class="status {{ status.value }}">{{ status.label }}</span>
    <a href="{{ post.link }}" class="thumbnail">
    {{ post.meta('photo') | picture }}
    </a>
    <p class="title">
        <a href="{{ post.link }}">
            {{ post.title }}
        </a>
    </p>
    {% if general_fields.shop.show_rating %}
    {% include 'overall/rating.twig' %}
    {% endif %}
    <div class="columns">
        <div class="col">
            <p class="price">
                <span>{{ price }}</span>
                <span>₴</span>
            </p>
        </div>
        <div class="col">
            {% if status.value == "in_stock" %}
            <button data-id="{{ post.id }}" class="add_to_cart">
                <svg>
                    <use href="{{ svg_sprite }}#iconCart"></use>
                </svg>
                <span>Купити</span>
            </button>
            {% endif %}
        </div>
    </div>
</div>
