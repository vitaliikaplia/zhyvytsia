<label for="{{ label }}">
    <span class="label">{{ description }}</span>
    <input type="number" class="widget-options-number" name="{{ label }}" id="{{ label }}" value="{{ value }}" step="1" />
</label>
