<label for="{{ label }}">
    <span class="label">{{ description }}</span>
    <input type="text" class="widget-options-text" name="{{ label }}" id="{{ label }}" value="{{ value }}" autocomplete="off" placeholder="{{ val.placeholder }}" />
</label>
